
export interface GrantMilestone {
    id: string;
    proposalId: string;
    description: string;
    deadline: string;
    address: string;
    txHash: string;
    createdAt: string;
    updatedAt: string;
}